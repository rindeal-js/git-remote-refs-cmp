{
  "name": "@rindeal/git-remote-ref-compare",
  "version": "0.1.0",
  "license": "GPL-3.0-only OR GPL-2.0-only",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx -- tsc --pretty && npx -- terser dist/index.js -o dist/index.min.js --source-map",
    "test": "npx -- jest --color",
    "lint": "npm run -- lint:eslint",
    "lint:eslint": "npx -- eslint --color -- src/",
    "lint:reuse": "pipx run --quiet -- reuse lint"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.1",
    "@tsconfig/node20": "^20.1.4",
    "@types/jest": "^29.5.12",
    "@types/node": "^22.5.2",
    "eslint": "^9.9.1",
    "globals": "^15.9.0",
    "terser": "^5.31.6",
    "ts-jest": "^29.2.5",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.4.0"
  }
}
